import 'reflect-metadata';

describe('CreateExpenseInMonthService', () => {
  beforeEach(() => {});

  it('should be able to create the amount of expenses related to the installments', () => {});
});
